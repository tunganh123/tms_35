<%= form_for current_user.user_subjects.build, remote: true do |f| %>
  <div><%= hidden_field_tag :subject_id, subject.id %></div>
  <%= f.submit t("status.start"), class: "btn btn-primary" %>
<% end %>